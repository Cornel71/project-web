# Cornel-Mihai Badea — Site (K8s + ArgoCD + Traefik + cert-manager)

A personal presentation website, deployed to Kubernetes via ArgoCD (GitOps),
routed through Traefik, and served over HTTPS using a self-signed certificate
from cert-manager.

